'use client';

import { FormattedMessage } from "react-intl";

export default function ExampleClientComponent() {
    return (
        <h2>
            <FormattedMessage id="homepage_subheader"/>
        </h2>
    );
}